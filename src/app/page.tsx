import DefaultLayout from '@/layout/defaultLayout';
import React from 'react';

const Home = (): JSX.Element => (
	<DefaultLayout>
		<section>
			<h2>essa e a home</h2>
		</section>
	</DefaultLayout>
);
export default Home;
